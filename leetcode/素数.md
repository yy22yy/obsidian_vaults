


## 判断是否是素数

```cpp
返回是否是素数-- true or false
bool isPrime(int x){
    if(x < 2) return false;
    for(int i = 2;i*i <= x;i++)
        if(x % i == 0) return false;
    return true;
}
```

## 将一定范围内的素数存入数组

```cpp
vector<int> prime(q+1,0);
prime[1]=2;prime[2]=3;
for(int i=3;i<=n;i+=2){
	if(ifPrime(i)){
		prime.push_back(i);
	}
}
```