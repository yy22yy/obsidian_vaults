
```cpp
long long comb(int n, int r) {
    if (r > n) return 0;
    if (r > n - r) r = n - r;//r始终为较小者，利用对称性统一公式
    long long res = 1;
    for (int i = 0; i < r; i++)
        res = res * (n - i) / (i + 1);//i+1从(1,r);n-i从(n,n-r+1);
    return res;
}
```

对于阶乘 （n,r）和(n,n-r)的结果相等；
始终另r为二者中的较小值；
