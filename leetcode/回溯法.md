
- 切割、组合问题可以利用回溯法，因为可以抽象成一个树形结构；

![[Pasted image 20260312121430.png]]


# 回溯法模板

```cpp
void backtracking(参数) {
    if (终止条件) {
        存放结果;
        return;
    }

    for (选择：本层集合中元素（树中节点孩子的数量就是集合的大小）) {
        处理节点;
        backtracking(路径，选择列表); // 递归
        回溯，撤销处理结果
    }
```

# 回溯解决组合问题 1
![[Pasted image 20260312123140.png]]

# 回溯题中的去重

```cpp
class Solution {

private:

    vector<vector<int>> result;

    vector<int> path;

    void backtracking(vector<int>& candidates, int target, int sum, int startIndex) {

  

        if (sum == target) {

            result.push_back(path);

            return;

        }

  

        if(sum > target) return ;//提前剪枝

  

        for (int i = startIndex; i < candidates.size() && sum + candidates[i] <= target; i++) {

            // 要对同一树层使用过的元素进行跳过

     =>去重代码  if (i > startIndex && candidates[i] == candidates[i - 1]) {


                continue;

            }

            sum += candidates[i];

            path.push_back(candidates[i]);

            backtracking(candidates, target, sum, i + 1); // 和39.组合总和的区别1，这里是i+1，每个数字在每个组合中只能使用一次

            sum -= candidates[i];

            path.pop_back();

        }

    }

  

public:

    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {

        path.clear();

        result.clear();

        // 首先把给candidates排序，让其相同的元素都挨在一起。

        sort(candidates.begin(), candidates.end());

        // 这个算法中的i其实表明了当前选中了多少个元素；

        // 算法中的startindex表明了在原数组中从第几个元素开始选取

        // if (i > startIndex && candidates[i] == candidates[i - 1]) 表示后面使用的前面已经用过

        backtracking(candidates, target, 0, 0);

        return result;

    }

};
```



回溯算法其实只有 **三个动作**：

```text
1 选择  
2 递归  
3 撤销选择
(任意顺序与指定顺序的差异在于)
```

代码对应：

```cpp
used[i] = true;  
path.push_back(i);  
  
backtrack(...);  
  
path.pop_back();  
used[i] = false;
```

- 经典例题：[[P1706 全排列问题]]
- LC 78：[[78 子集]]
- 

![[Pasted image 20260308124851.png]]