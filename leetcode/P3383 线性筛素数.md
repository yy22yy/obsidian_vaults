
# 取余实现

```cpp
bool isPrime(int x){
    if(x < 2) return false;
    //用常规取余的方法 
    for(int i = 2;i*i <= x;i++)
        if(x % i == 0) return false;
    return true;
}

//  gpt素数处理
    for(int i=2;i<=n;i++){
        if(!vis[i]) prime.push_back(i);

        for(int p:prime){
            if((long long)i*p > n) break;
            vis[i*p] = true;
            if(i%p==0) break;
        }
    }

```

# 初始写法


```cpp
#include<bits/stdc++.h>
using namespace std;
bool isPrime(int x){
    if(x < 2) return false;
    //用常规取余的方法 
    for(int i = 2;i*i <= x;i++)
        if(x % i == 0) return false;
    return true;
}
int main(){
    int n,q;cin>>n>>q;
    vector<int> k(q+1,0);

    //素数处理
    vector<int> prime;
    prime.push_back(0);prime.push_back(2);
    for(int i=3;i<=n;i+=2){
	if(isPrime(i)){
		prime.push_back(i);
	}
}
    
    for(int i=1;i<=q;i++){
        cin>>k[i];
    }
    sort(k.begin(),k.end());

    for(int i=1;i<=q;i++){
        cout<<prime[k[i]]<<endl;
    }
}
```





# chatgpt 写法

```cpp
#include <bits/stdc++.h>
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n,q;
    cin >> n >> q;

    vector<int> prime;
    vector<bool> vis(n+1,false);
	
	
	//质数处理部分
    for(int i=2;i<=n;i++){
        if(!vis[i]) prime.push_back(i);

        for(int p:prime){
            if((long long)i*p > n) break;
            vis[i*p] = true;
            if(i%p==0) break;
        }
    }
	
	//直接请求输入和输出写法
    while(q--){
        int k;
        cin >> k;
        cout << prime[k-1] << '\n';
    }
}
```