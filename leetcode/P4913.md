

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {

    //树的状态取决于输入状态
    int n;
    cin >> n;
    vector<int> arr(2*n+1,0);
    arr[0]=1;
    int max_i = 0;

    int a,b;
    for(int i=1;i<n+1;i++){
        //n次循环输入
        cin>>a>>b;
        //左右子树的关系
        if(a!=0 || b!=0)
        {
            max_i = i;
        }
    }
    //计算max_i所在层数
    max_i = 2* max_i +1;
    int level = log2(max_i) + 1;
    cout<<level;
    //无子节点
    
	return 0;
}
```