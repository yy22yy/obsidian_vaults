
# 精度问题

==涉及除法，首选double==

```
int a,b;
cin>> a >> b;
强制类型转换的方法
double w= (double) v/m;

```

# [[结构体]]排序问题

```cpp
struct Node{
	int a;
	double b
};

排序依据
sort(Node.begin(),Node.end(),[](const Node&a,const Node&b){
	return a.num < b.num;
});

a是小的值，排序是依据num的值升序（从小到大排列）

```


# 输出精度问题

```cpp
cout << fixed << setprecision(小数点后的位数) << a;
cout << fixed << setprecision(2) << a;
```


# 数组的常见问题[[排序sort]][[查找]]

```cpp

依次查找数组中的最小值
for(int i=0;i<n;i++){
        int x = *min_element(arr.begin(),arr.end());
        int pos = min_element(arr.begin(), arr.end()) - arr.begin();
        //下标寻找
        cout << pos+1<< " ";
        arr[pos] = INT_MAX;
        total_time += x*(n-i-1);
    }
```

```cpp
无穷大的常见赋值方法
int a =INT_MAX;
```

# 哈夫曼树求和[[哈夫曼算法]]

