
[[P2141 NOIP 2014 普及组珠心算测验]]

unordered_set -- 集合的定义方法；
unordered_map -- 哈希表的定义方法；

![[Pasted image 20260306195617.png]]

```cpp
unordered_set<int> numSet;

//插入与删除
numSet.insert();
numSet.erase();

//集合查找
numSet.find();

//大小与判空
numSet.size();
numSet.empty();

```

```cpp
if (numSet.count(sum) \ && !found.count(sum)) { 
 count++; 
 found.insert(sum); 
  }
  
  集合: [1, 2, 3, 4, 5]

1+4=5  ┐
2+3=5  ┘  → 5 只被计数一次

1+2=3      → 3 计数一次

答案 = 2
  
  
```