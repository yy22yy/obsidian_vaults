
- vector in t创建 [[矩阵]] 的两种方法

```cpp

vector<vector<int>> (嵌套向量)

vector<int>(n * n) (一维化向量)


```

- 循环求和
```cpp
//对整个数组求和，初始值为0
accumulate(num.begin(),num.end(),0)
```

### `matrix.size()`求的是什么？

对于 `std::vector<std::vector<int>> matrix`：

```cpp


矩阵默认或者首先选中的对象是行对象
选中行对象后，下一个是列对象

matrix.size() = 行数
matrix[i].size() = 第 i 行的列数
```

# 数组中选取部分元素的子数组排列组合

